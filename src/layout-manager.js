/**
 * Layout Manager
 * Simple fixed layout - no layout options
 */

/**
 * Initialize layout on page load
 */
export function initializeLayout() {
    // Fixed layout, no need to apply anything
    console.log('Layout initialized: Fixed 3-row layout');
}

/**
 * Show layout selector modal (disabled)
 */
export function showLayoutSelector() {
    // No layout options available
    alert('Layout is fixed: Power on top, HR and Cadence in middle, Power Averages at bottom');
}
